---
interface Props {
	title: string;
	id: string;
}

const { title, id } = Astro.props;
---

<section class="mt-10 border-t border-border-subtle pt-10" aria-labelledby={id}>
	<h2 id={id} class="mb-6 text-xl font-semibold text-foreground">{title}</h2>
	<slot />
</section>
