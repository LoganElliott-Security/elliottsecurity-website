/**
 * Theme constants for contexts that cannot read CSS variables
 * (e.g. <meta name="theme-color">, web manifest mirrors).
 * Keep in sync with --color-bg in src/styles/tokens.css (dark theme).
 */
export const THEME_COLOR_DARK = '#09090B';
